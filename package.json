{
  "name": "orchestrator-public-usecase",
  "version": "1.0.0",
  "description": "orchestrator public use case: AUT has a url (localhost or public ip)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "0xIslamTaha",
  "license": "ISC",
  "devDependencies": {
    "@0xislamtaha/orchestrator": "^1.0.1",
    "cypress": "^7.3.0",
    "mocha": "^8.4.0",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.2.0"
  }
}
